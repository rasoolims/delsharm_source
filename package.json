{
  "name": "delsharm_blog",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
   "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
"deploy": "npm run build && rsync -av --delete --exclude='.git' --exclude='.nojekyll' --exclude='staticman.yml' dist/ ../delsharm/ && cd ../delsharm && git add . && git commit -m 'Auto-deployed update' && git push"  },
  "dependencies": {
    "@astrojs/mdx": "^7.0.2",
    "@astrojs/rss": "^4.0.19",
    "@astrojs/sitemap": "^3.7.3",
    "astro": "^7.0.6",
    "sharp": "^0.35.0"
  }
}